import { PlaceholderPage } from "@/components/ui/placeholder-page";

export default function CatalogosPage() {
  return (
    <PlaceholderPage
      title="Catálogos"
      description="CRUD de posiciones y categorías (solo admin)."
    />
  );
}
