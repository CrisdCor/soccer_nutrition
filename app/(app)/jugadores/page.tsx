import { PlaceholderPage } from "@/components/ui/placeholder-page";

export default function JugadoresPage() {
  return (
    <PlaceholderPage
      title="Jugadores"
      description="CRUD de jugadores (crear, editar, listar, inactivar) — siguiente módulo del MVP."
    />
  );
}
